Write a Python function parse_config(config_string: str) -> dict. It must take a multiline string of key-value pairs separated by an equals sign (=) and return a dictionary.

Ignore any lines starting with # and any empty lines.

Strip leading and trailing whitespace from both keys and values.

Constraint: Do not import any external or standard libraries (e.g., re, json).