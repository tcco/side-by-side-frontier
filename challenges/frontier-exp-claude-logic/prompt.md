I have a complex, messy Python script that calculates the pricing for a multi-tiered SaaS product with grandfathered plans, pro-rated mid-month upgrades, and region-specific tax logic. I need you to refactor this logic entirely from scratch in TypeScript.

You must perfectly adhere to the following structural rules:

Use purely functional programming (no mutable state, no classes, only pure functions).

Implement the core logic using a State Monad.

Every function must have exhaustive TypeScript interfaces for inputs and outputs.

Do not use any external dependencies or libraries.

You must include mathematically rigorous boundary testing logic for floating-point currency conversions.

All function names must follow strict PascalCase, and variables must be camelCase.

Write the complete, refactored TypeScript code
