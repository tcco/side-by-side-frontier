Implement a thread-safe, lock-free, single-producer single-consumer (SPSC) Ring Buffer (Circular Queue) in C++11 or Rust from scratch. The buffer must have a fixed capacity allocated on initialization. Use atomic load/store operations with explicit memory ordering (memory_order_acquire and memory_order_release) to synchronize the read and write pointers without any mutexes or condition variables. To avoid false sharing, ensure that the read and write pointer indices reside on separate cache lines using proper alignment. Provide the full implementation including push and pop methods that return boolean flags indicating success or failure without blocking.