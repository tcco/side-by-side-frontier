Write a Python class MovingAverage that calculates the simple moving average of a data stream. The constructor should take an integer window_size.
Include a method add(val: int) -> float that adds a new value to the stream and returns the current moving average.
Constraint: The add method must operate in $O(1)$ time complexity.