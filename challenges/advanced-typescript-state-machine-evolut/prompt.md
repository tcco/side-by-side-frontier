Implement an immutable, purely functional Redux-like state reducer in TypeScript that manages a complex multi-step checkout workflow. The state must transition through: Idle $\rightarrow$ CartValidated $\rightarrow$ PaymentPending $\rightarrow$ InventoryReserved $\rightarrow$ Completed.Add a strict constraint: If a CANCEL action occurs during PaymentPending, the system must transition to a Rollback state that triggers an asynchronous reverse-inventory sweep before returning to Idle. Provide the exact TypeScript types ensuring that invalid transitions (e.g., directly from Idle to Completed) cause compile-time errors, not just runtime checks.