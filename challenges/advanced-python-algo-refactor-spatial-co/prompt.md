You are handed a legacy Python function that computes the intersections of millions of 2D bounding boxes using a naive $O(N^2)$ double-loop. Refactor this code to run in $O(N \log N)$ time using a Sweep-Line algorithm or a spatial data structure (like an R-Tree or Quadtree). The solution must be implemented completely from scratch using only the Python standard library (no scipy or shapely). Optimize for memory footprint, ensuring that objects outside the current sweep band are immediately eligible for garbage collection.